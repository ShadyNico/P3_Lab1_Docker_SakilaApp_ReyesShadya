@model IEnumerable<SakilaApp.Models.FilmCategory>

@{
    ViewData["Title"] = "Film Categories";
}

<div class="page-header">
    <h1>Film Categories</h1>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</div>

<div class="table-responsive app-table">
<table class="table table-hover align-middle">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LastUpdate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Film)
            </th>
            <th class="table-actions-cell">Actions</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.CategoryId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Film.FilmId)
            </td>
            <td class="table-actions-cell">
                <div class="table-actions">
                    <a asp-action="Edit" class="btn btn-sm btn-outline-primary">Edit</a>
                    <a asp-action="Details" class="btn btn-sm btn-outline-secondary">Details</a>
                    <a asp-action="Delete" class="btn btn-sm btn-outline-danger">Delete</a>
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
</div>

<partial name="_Pagination" />
